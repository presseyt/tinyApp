<div style="height:80px; background-color:silver; position: relative; ">
  <div style="display: inline-block; vertical-align: middle;">
    <h1 style="font-family: 'Montserrat', sans-serif;"><a style="color:black; text-decoration: none;" href="/urls">Tiny App</a></h1>
  </div>
  <div style="float:right; margin-top: 5px; margin-right: 5px;">
    <form method="POST" action="/login">
      <% if (!username){ %>
        <input type="text" name="username">
        <button>Login</button>
      <% } else { %>
        <!-- The instructions said to POST to /logout (if the logout button is clicked) but this is functional so I'm leaving it. -->
        Current User: <%=username%>
        <button name="username" value="">Logout</button>
      <% } %>
    </form>
  </div>
</div>

<div style="clear:both;"> &nbsp </div>